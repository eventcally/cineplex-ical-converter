# Cineplex ical converter

## Install

```sh
python3 -m venv env && source env/bin/activate
(env) pip install -r requirements.txt
```

## Docker

### Build image

```sh
docker build -t eventcally/cineplex-ical-converter:latest .
```
